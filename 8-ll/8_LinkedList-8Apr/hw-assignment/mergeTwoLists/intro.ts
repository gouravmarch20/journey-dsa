// 21. Merge Two Sorted Lists
// w1 : A //*O(n+m)
/**
    //~ new node start with -1  
    - 1st while loop until one list not equal to null

    - after null simply pending list
    //~ return new node head 1 step ahead to skip -1 
 */
// w1 : B
/**
    //~ new node start small value of l1 / l2
    - 1st while loop until one list not equal to null

    - after null simply pending list connect 
    -return 

 */


//& merge using using recursion --> by  backtracing or return
///& response we are joining because we call smaller data/val each time so above link always look less
