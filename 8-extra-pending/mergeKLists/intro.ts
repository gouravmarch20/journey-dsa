// 23. Merge k Sorted Lists
// naive //* O(n k)
/**
 * 
    - merge 1st , 2nd list  
    - merge 3 rd list , with result of (merge1st , merge2nd) list

    - merge 4th -> with result of merge(1,2,3) 
 */
// * O(n log(N))
// iterate whole , sort the linked list
// * O(n log(k))
//merge sort
//heap

