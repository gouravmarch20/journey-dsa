// Add One Row to Tree
//^ understanding : stop 1 node before depth , on that node add new node left / right assign
//! this is not question want relink --> newNode->left sai left , newNode->right = prev right

// ? we link parent->left :: (newNode)->left :: prev Left
// ? we link parent->right :: (newNode)->right ::  prev right