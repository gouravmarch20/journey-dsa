// 744. Find Smallest Letter Greater Than Target
// ! question reading miss already sorted 
// *O(n) -> brute
/**
  - ans = false
  if next greater than target found --> assign to ans
  else at end assign s[0]
 */