//1910. Remove All Occurrences of a Substring


//~ find -> O(S1 * S2 ) --> size of both string 
// leftPart -> O(S1)
// rightPart -> O(S1)
// concat-> O(S1)

//~ no of recursive call : O( S1 / S2)

//* O(S1 ^ 2) --> O(N^2)
//& no of recursive call : O( S1 / S2)  --> O(N/M)